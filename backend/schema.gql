type Query {
  userNeeds: [UserNeed]!
  userNeed(id:ID!): UserNeed
}

type Mutation {
  addUserNeed(name: String, description: String): UserNeed!
  deleteUserNeed(id:ID!): ID!
}

type UserNeed {
  id: ID!
  name: String
  description: String
}